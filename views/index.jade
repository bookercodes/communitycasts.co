extends base

block vars
  - var title = "Home"

block body
  table.table
    thead
      tr
        th Title
        th Channel
    tbody
      each video in videos
        tr
          td 
            a(href="#{video.url}") #{video.title}
          td #{video.channelName}